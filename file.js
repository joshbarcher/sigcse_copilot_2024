// JavaScript

function declareVariables() {
    /*
    This function should declare two float variables named `temp_1990` and `temp_2020`,
    representing the average global temperatures in the years 1990 and 2020 respectively.
    It should also declare a float variable named `temp_change` and use it to store the
    difference in average global temperatures between 1990 and 2020.
    */
}

function printValues() {
    /*
    This function should print the values of `temp_1990`, `temp_2020`, and `temp_change`.
    */
}

function analyzeTempChange() {
    /*
    This function should use conditional statements to analyze `temp_change`.
    Depending on the value of `temp_change`, it should print different messages.
    */
}

function main() {
    /*
    This is the main function that calls the other functions.
    */
    declareVariables();
    printValues();
    analyzeTempChange();
}

main();